# -*- coding: utf-8 -*-
"""新しい工房のひな形。genres/<名前>.py にコピーして中身を入れ替える。

ファイル名が「_」で始まるものは工房として読み込まれない（このひな形は出てこない）。
"""

# ── 必ず書く3つ ───────────────────────────────────
TITLE = "新しい工房"          # 画面に出る名前
PORT = 8520                   # 他と重ならない番号（既存は 8511〜8515）
VOCAB = {
    # "カテゴリ名": [(日本語, イラスト用タグ, 写実用の語 or None), ...]
    #   3つめが None ならイラスト用がそのまま写実にも使われる。
    #   日本語が同じでも英語の書き方が変わるものは、必ず両方書く。
    "主役": [
        ("見本A", "sample tag a", "a sample subject"),
        ("見本B", "sample tag b", None),
    ],
    "場所": [
        ("見本の場所", "sample place", "at a sample place"),
    ],
    # 動画モードで使うなら「主役の動き」を作る（写実用だけ埋めればよい）
    "主役の動き": [
        ("ほとんど動かない", None, "almost still, only subtle movement"),
    ],
}

# ── 書かなくてよいもの（省略すると自動で決まる）─────────
ICON = "🎨"                   # 省略時 🎨
EXAMPLE = "文章で書く例"       # 入力欄の薄い文字
PUBLISH = False               # False なら公開版(docs/index.html)に載らない

# ALWAYS   … 常時表示するカテゴリ。省略すると先頭2つ（動画は＋主役の動き）
# ROLL_CHANCE … おまかせの採用確率。省略すると先頭2つが1.0、残りは0.6
# NO_HUMAN / HUMAN_ONLY_CATS / NO_HUMAN_MOTION … 人物なしの主役があるときだけ
# ALIASES  … {正式ラベル: (言い換え, ...)}。照合にだけ使い、プロンプトには出ない
